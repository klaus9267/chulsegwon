# 이력

무엇을 **언제** 했는지. 왜 그렇게 했는지는 [HANDOFF.md](HANDOFF.md) 의 ADR 에,
앞으로 할 일은 [TASKS.md](TASKS.md) 에 있다.

## 적는 법

- 과제를 끝내면 맨 위 "최근" 에 한 줄 적는다: `날짜 · T-xx · 무엇 · 커밋` 과 **한 줄짜리 교훈**(있으면)
- 여러 커밋이 한 흐름이면 묶어서 적는다. 커밋 목록 전체는 `git log` 가 이미 갖고 있다 —
  여기는 **흐름과 숫자**를 남기는 곳이다
- 공개 사이트에 반영된 시점은 **공개본** 표시를 붙인다. 로컬 `dev` 에만 있는 것과 구분해야 한다

---

## 지금 공개된 것

| | 커밋 | 날짜 |
|---|---|---|
| 사이트 (GitHub Pages, `main`) | `7562cce` | 09-15 |
| 도달권 데이터 (릴리스 `data-latest`) | 20.5MB · 역 657 · 동네 1,768 | 09-15 21:16 |
| 자동 공개 | **켜짐** — 배치 매 회차 끝, 바뀌었고 코드가 `main` 과 같을 때만 | 09-15 부터 |
| 원격 `dev` | `main` + 기록 커밋 | 09-15 |

---

## 최근

<!-- 새 항목은 여기 위에 쌓는다 -->

- **09-15** · T-20 · **출발지를 바꾸면 동네 라벨이 안 뜨던 문제.** 라벨을 화면 범위로 솎는데
  다시 뽑는 시점이 줌 변경·사용자 끌기뿐이었다 → `idle` 에서 다시 뽑는다. `7562cce` (공개본)
  - **교훈: 원인을 두 번 틀렸다.** 코드를 읽고 세운 가설(`run()` 경로)은 `<form>` 이 없어서 틀렸고,
    계측으로 세운 가설(애니메이션 경쟁)은 재현 스크립트가 해시만 바꿔 **새로고침을 안 해서** 틀렸다.
    둘 다 "그럴듯한 이야기"였다
  - **교훈: 증상을 본 환경부터 의심할 것.** 앱 안 브라우저 패널이 숨겨져 있어
    `requestAnimationFrame` 이 초당 0번이었다. 카카오맵 애니메이션이 멈춘 채로 "지도가 안 따라간다"를 봤다
  - **교훈: 고치기 전 코드로 검사부터 돌릴 것.** 그 덕에 첫 수정(easeTo 제거)이 오히려 실패를 6 → 9건으로
    **늘린다**는 걸 잡았다 — easeTo 가 줌을 흔들어 우연히 라벨을 다시 뽑게 하고 있었다. 틀린 주석까지 코드에
    남길 뻔했다
  - 회귀 검사 `tools/check-map-follow.mjs` (헤드리스 Edge · 다섯 경로 · 공유 링크 새로고침을 기준으로)

- **09-15** · T-03 · **공개를 자동으로.** 배치가 새 행렬을 만들면 자산을 바꾸고 사이트까지 다시 배포한다
  (사람 확인 없이 — 사용자가 없는 사이드 프로젝트라). `tools/publish.py`
  - 막는 것: 데이터가 덜 됐으면 · 코드가 `main` 과 다르면 · 안 바뀌었으면 · 배포가 실패하면 기록 안 함
  - **교훈:** 빌드가 중간에 죽으면 파일 **개수는 맞는데 내용이 섞인다.** 그래서 개수 대신
    "매니페스트가 행렬보다 나중에 써졌는가"를 본다 — `DongMatrix` 가 매니페스트를 맨 끝에 쓰기 때문이다
  - **교훈:** 배치는 **Windows PowerShell 5.1** 이라 파이썬 출력이 CP949 로 나간다. 메시지의 `—` 하나로
    `UnicodeEncodeError` 가 나서 공개가 매번 죽었을 것이다. Git Bash 에서 `PYTHONIOENCODING=utf-8` 을 붙여
    시험하면 안 보인다 — **돌 환경 그대로** 불러봐야 한다

- **09-15** · T-01 · T-02 · **9월 7~9일 작업을 처음 공개했다.** 푸시 42커밋 → 데이터 자산 12 → 21MB →
  `main` fast-forward → Pages 배포. 공개본에서 운정중앙(GTX-A)을 골라 확인. `ec3ac7b`
  - **교훈:** ADR-12 에 적힌 업로드 명령이 틀려 있었다(`data.tgz` 로 올리면 자산이 하나 **더** 생길 뿐
    워크플로가 받는 `chulsegwon-data.tgz` 는 안 바뀐다). 문서대로 했으면 "배포 성공"인데 옛 데이터였을 것이다
  - **교훈:** 순서가 있다 — **데이터 먼저, `main` 나중.** 반대로 하면 새 코드가 옛 데이터(역 621축)를 받아 깨진다
  - 올리기 전에 `/chulsegwon/` 하위 경로로 로컬 빌드해서 띄워봤다. `main` 을 되돌리려면 강제 푸시가
    필요하니, 되돌릴 일을 안 만드는 게 싸다. 카카오맵은 **5173 포트에서만** 뜬다(등록된 도메인)
  - 확인하다 원래 있던 결함을 찾았다 — 이름 치고 Enter 면 지도가 안 따라간다(T-20)

- **09-15** · 과제 목록(`TASKS.md`)과 이력(`HISTORY.md`)을 `HANDOFF.md` 에서 떼어냈다.
  `HANDOFF.md §6 열린 항목` 은 대부분 해결된 채로 남아 있었고 `§9 이력` 은 08-30 에서 멈춰 있었다

---

## 09-09 — 정확도를 재다

하루 동안 **"우리 등시선이 맞는지 어떻게 아나"** 에 답했다. 참값 없이 스스로 모순을 찾는 길과,
카카오와 문앞-문앞으로 대는 길 둘 다 갔다. 그리고 모델을 정확하게 만들 때마다 대조 지표가
나빠지는 일을 **세 번** 겪었다.

| 커밋 | 무엇 | 숫자 |
|---|---|---|
| `4007873` | 등시선 오차 다섯 가지 + **GBIS 마을버스** 892개 노선 | 답을 그림에서 되읽어 동네 48% 누락이 있었다. 정류장 59,913 → 70,143 |
| `7e73def` `fde51b0` | 버스 배차를 **시간대별로** — 운행 대수에서 역산 | 서울·경기·인천 |
| `6fb628b` | **참값 없이** 잡은 버그 넷 — 승차 여유가 역방향(출근 슬롯 전부)에서 하차 뒤에 붙어 있었다 | 미러 정합 위반 79/120 → 1/200 |
| `e0b5ed8` | 카카오 **문앞-문앞 OD 46개** 대조, `--mode odcheck` | 정밀도 0.95 · 재현율 0.57 · 1호선이 두 토막으로 끊겨 있었다 |
| `915b603` `663741f` | 합성 시간표를 버리고 **서울교통공사 실측 시각표** (1~9호선) | 합성이 강남 2호선을 78%, 수원 1호선을 93% 과잉 운행시키고 있었다 |
| `8e68220` | 규격 검사가 잡은 결함 셋 | 동대문역사문화공원에 2·4·5호선 미정차 · 14km 를 30초에 가는 유령 구간 · 자정 넘김 |
| `0f99c70` | 노선망을 **OSM** 으로 | GTX-A·신림선·대곡소사선·별내선·진접선 등 2020년 이후 개통분 10종. 일산→온수동 76 → 50분(카카오 51) |
| `f4cc589` | **코레일 광역철도 실측 시각표** (철도데이터포털) | 실측 노선 9 → 14개. 편수가 원자료와 정확히 일치 |
| `6a3321e` `d119051` | 루트에 떨어진 GBIS 엑셀 치움 · 매니페스트 경고문의 노선 수 제거 | |

**교훈:** "공개 자료가 없다"는 판단을 세 번 했고 세 번 다 틀렸다. 찾는 데를 안 찾았다 —
포털은 목록이지 저장소가 아니다([ADR-40](HANDOFF.md#adr-40-없다던-코레일-시각표가-있었다--찾는-데를-안-찾았을-뿐-2026-09-09)).

**교훈:** 모델이 정확해질 때마다 지표가 나빠진 건 **상쇄하던 오차를 걷어낸 것**이었다.
판단은 카카오가 아니라 원자료와의 편수 대조로 했다. 다만 지표가 무엇을 재는지는 아직 못 갈랐다([T-04](TASKS.md#t-04)).

---

## 09-07 ~ 09-08 — 버스를 넣다

지하철만 보던 도구에 **버스·도보를 한 망으로** 넣었다. 수도권 버스 GTFS 가 없어서 직접
만들었고, 만든 걸 카카오맵으로 대조하며 스케줄로 계속 다시 맞추게 했다.

| 커밋 | 무엇 | 숫자 |
|---|---|---|
| `6eb4710` | 수도권 **버스 GTFS 를 직접 생성** + 수집 스케줄 | 없어서 만들었다 |
| `01bf97b` | 버스 소요시간 30% 과소 → 정류장 통과 비용 20 → 58초 | 카카오 대조로 드러남 |
| `8a1bbc3` `73981fb` | 보정을 상수에서 빼내 **스스로 다시 맞추게** | 평가셋을 반으로 갈라 자랑과 검증을 분리 |
| `a297235` | 광역·직행좌석 속도 보정 + 물리적 상한 | |
| `2da1cca` `e8df5a9` | 버스가 **서지 않는 형상점**을 정류장으로 세고 있었다 | 30,173행 제거 |
| `a3f06cb` | 속도 관측 필터가 고속 구간을 통째로 버리고 있었다 | 80km/h 일괄 → 길이별 |
| `f4bc3fd` `14ed757` | 편도 순환 노선을 왕복으로 오인 · 순환선이 절단점을 못 지나감 | |
| `ede3fb0` | 결론 25건을 반박시켰더니 **24건이 뒤집혔다** | ADR-29 |
| `e5201f4` | 스냅샷이 자정에 한도를 다 먹어 낮·퇴근 시간대가 한 번도 안 찍혔다 | |
| `5d20460` `c40f83b` `49fbfd4` | 지하철도 GTFS 로 · **승차시간을 처음 직접 쟀다** | 8.8% 빨랐다 → 역당 정차 10초 |
| `b46c82f` | 지하철·버스·도보가 한 망에서 도는 첫 탐색 — **RAPTOR** | |
| `432d4ab` `c3c337e` | **도착 축을 역 621개에서 동네 1,768개로** · 웹이 새 행렬을 읽는다 | 버스가 들어오면 "가까운 역"이 의미를 잃는다 |
| `9176106` | 인천 버스 — 상수는 선언돼 있었는데 아무도 안 썼다 | |
| `9c393ce` | 접근·이탈 도보를 직선거리에서 **실제 도보망**으로 | OSM 476만 노드 |
| `1b44d76` | 스케줄이 도달권 행렬까지 다시 만든다 · GTFS 재현 가능하게 | 내용 해시로 판단 |

---

## 09-01 ~ 09-06 — 자취 도구로

| 커밋 | 날짜 | 무엇 |
|---|---|---|
| `91a8458` | 09-01 | **자취 타겟으로 전환** — 동네 시세·편의시설 조건·공유 링크 |
| `c4668cf` | 09-01 | 먼 역 하나가 화면 배율을 정하지 않도록 |
| `301679d` | 09-01 | 확대하면 개별 건물 실거래 — 빌라·오피스텔 62,243동 |
| `c990591` | 09-03 | 기술 선택의 이유 — `STACK.md` 신설, README 개편 |
| `1c4d8a9` | 09-06 | 역 목록 겹침 · 색만으로 시간을 못 읽던 문제 |
| `86299c8` | 09-06 | 공유 미리보기 · 역과의 고도차 · 시세 추이 · 동네 비교 — 09-15 까지 공개본 |

---

## 08-30 ~ 08-31 — 배포와 방향 전환

| 커밋 | 날짜 | 무엇 |
|---|---|---|
| `3e08724` | 08-30 | 인수인계 문서 — 결정 근거와 이력 |
| `c9f54b2` | 08-30 | 등시선이 역마다 따로 놀던 문제 |
| `e8b1757` `8042f7f` | 08-30 | 카카오맵 지원 — 지도 어댑터 분리 |
| `9c5a0fa` | 08-30 | 프로젝트명을 **출세권**(chulsegwon)으로 |
| `4ac3a09` `2635342` | 08-30 | GitHub Pages 자동 배포 · ADR-12 |
| `20aa343` | 08-30 | 범례 구간별 분 표시 + 역 그래프 최신화 시도 기록(Wikidata, 중단) |
| `013bd9d` `d1ccada` | 08-30 | 등시선 구간 경계선 · 첫 화면에서 바로 도달권 |
| `a2b52a9` | 08-30 | 다중 출발지 교집합 + 네이버부동산 핸드오프 |
| `3bfd0d3` `8aa3a90` `c6998d5` | 08-31 | 실거래가 수집 배치 · 역 마커 재설계 · "40분 안, 전세 N억 이하 단지" |

---

## 08-21 ~ 08-30 — 시작

| 커밋 | 날짜 | 내용 |
|---|---|---|
| `fcf4cbd` | 08-21 | 도달권 계산기 + 지도 웹앱 |
| `d4512ef` | 08-21 | 카카오맵 대조 검증 + 저녁 배차 보정 |
| `3b31399` | 08-21 | 도달권 SVG 렌더러 |
| `67b3306` | 08-21 | 프로젝트명 homerange (이후 출세권으로 변경) |
| `f5e2636` | 08-22 | 배경지도 소실 + 원 겹침 수정 |
| `2ddd88e` | 08-23 | TDZ 버그 |
| `62b141f` | 08-23 | 레이어 미부착 시 render 크래시 |
| `2465eb5` | 08-27 | 서울 초기 화면 + 버튼 실행 방식 |
| `3b5c992` | 08-27 | 한글 지명 + 브이월드 옵션 |
| `742b783` | 08-30 | 역 콤보박스 |
| `75c05d2` | 08-30 | 선택 역 마커 + 포커스 |
| `472eda8` | 08-30 | 격자 → 등시선 |

원 설계 대화: <https://claude.ai/share/6fa09702-dec0-49fe-9d47-6566a755b341>
